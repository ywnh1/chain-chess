# ─── Python ───
__pycache__/
*.py[cod]
*.so
venv/
.env/
.venv/
env/
*.egg-info/
dist/
*.egg
pip-wheel-metadata/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# ─── Logs ───
logs/
log.log
*.log

# ─── Rust / Tauri ───
tauri/node_modules/
tauri/src-tauri/target/
tauri/src-tauri/gen/android/
tauri/src-tauri/gen/schemas/
tauri/src-tauri/.cargo/

# ─── Android / APK 构建 ───
*.apk
*.idsig
aapt2_arm64

# ─── 运行时 PID 文件 ───
*.pid

# ─── IDE / Editor ───
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ─── 环境变量 / 密钥 ───
.env
.env.*
!.env.example

# ─── 构建产物 ───
release/
release.keystore

# --- 字体（避免大文件进仓库） ---
tauri/public/fonts/font.ttf

# --- ML 训练数据（大量大文件，避免进仓库） ---
data/
*.jsonl

# --- XGBoost 训练生成的模型文件（可由 data/train_xgb.py 重新生成） ---
xgb_model.json
xgb_model_fast.json
xgb_model_board.json

# --- ai ---
.skills/
.skill/
prompt.md
.codewhale/

.html.bak
*.out

# ─── PWA / Wasm ───
# docs/pkg/ 是 wasm-pack 生成的发布版 Wasm（浏览器版），PWA 运行必需 —— 必须提交，不要忽略
# 注意：wasm-pack 每次运行会在 docs/pkg/.gitignore 写入 '*' 导致发布产物被忽略，
#       若重跑 wasm-pack 生成到 docs/pkg/，请删除该文件再提交 docs/pkg/ 内容
# Rust 编译中间产物（构建内容，不上传）
docs/wasm/target/
# Node 版 pkg（不上传，仅浏览器版 docs/pkg/ 需要上传）
docs/pkg-node/
