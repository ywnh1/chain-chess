# 要修改的内容
## 结算页面
1. 页面上两个图可点击全屏预览
2. 页面上两个图，修正它们的计算逻辑
3. 页面上两个图，都加一个白色的线，用来表示总数
4. 页面显示连爆最多的是谁触发的？连爆了几次
5. 结算页面只显示当前活着玩家的情况，死的玩家永远是 0，所以就不用显示了

## 游戏过程
1. 如果是 PVE（一人 vs 任意个 ai），允许玩家自选颜色，只有人或只有 AI 则不用自己选颜色，如果有几个人或几个 AI 也不需要
2. 允许在一局当中，玩家数为 0，也就是 ai 斗蛐蛐
3. 任意时刻，玩家都可以结束游戏，并且显示结算页面
4. 对于 tauri ，增加历史记录功能，此功能应该是一个列表页面，包括时间、模式、是否有 AI、AI 的模式、多少人、哪些颜色等等，以及其他结算页有的内容，这里也都要有

## 修复 bug
1. 在首次进入 AI 设置界面时，即使选的不是阿尔法贝塔剪枝，下面也会有一个深度
2. 其他修改

## 优化性能
1. 把更多的逻辑运算和代码挪到后端，使用 Rust 完成，性能再上一个台阶
2. 其他优化

## 优化 UI
1. 根据安装的 Skill 风格，对 UI 进行优化，总体暗色调进行保留
2. 其他优化

# 流程
1. 修改 python 和前端等
2. 让我进行预览
3. 按照我的要求重新修改
4. 重复 1~3 的内容
5. 可以后修改 tauri 相关代码
6. 编译并且给我测试
7. 全部完成之后，写 readme，把开源协议写到文件头
8. 推送云端

# 技术栈
保持不变，使用 python 加 FastAPI 作为后端，用 tauri 构建安卓项目
