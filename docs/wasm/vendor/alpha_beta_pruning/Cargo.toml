# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "alpha_beta_pruning"
version = "0.1.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A generic alpha-beta pruning search algorithm for adversarial game AI"
documentation = "https://docs.rs/alpha_beta_pruning"
readme = "README.md"
keywords = [
    "alpha-beta",
    "minimax",
    "game-theory",
    "search",
    "ai",
]
categories = [
    "algorithms",
    "game-development",
    "artificial-intelligence",
]
license = "MIT"
repository = "https://github.com/ywnh1/alpha_beta_pruning"

[lib]
name = "alpha_beta_pruning"
path = "src/lib.rs"

[features]
default = ["parallel"]
parallel = ["dep:rayon"]

[dependencies]
rayon = { version = "1.12.0", optional = true }
